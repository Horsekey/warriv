version: '3.8'

services:
  app:
    container_name: ${ENVIRONMENT}
    build:
      context: .
      args:
        - BOT_ENV=${ENVIRONMENT}
    env_file:
      - ${ENV_FILE}
    extra_hosts:
      - "host.docker.internal:host-gateway"
